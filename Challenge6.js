const  input= require('prompt-sync')();

const a = Number(input("entre un nombre reel "))
const b = Number(input("entre un nombre reel ")) 

console.log(a + b)
console.log(a - b)
console.log(a * b)
console.log(a / b)